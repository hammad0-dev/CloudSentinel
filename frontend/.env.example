# Copy to frontend/.env and adjust backend URL.



# Windows backend on same PC:

VITE_API_BASE_URL=http://localhost:3000/api



# Or Ubuntu backend only — use VM LAN IP (must match frontend + CORS on server):

# VITE_API_BASE_URL=http://192.168.141.128:3000/api


